import { ConvertirestadoPipe } from './convertirestado.pipe';

describe('ConvertirestadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirestadoPipe();
    expect(pipe).toBeTruthy();
  });
});
